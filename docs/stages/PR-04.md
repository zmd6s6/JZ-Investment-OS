# PR-04 — Thesis Engine & Shared Investment Memory

- State: `READY_FOR_REVIEW`
- Depends on: accepted PR-03 evidence pipeline, merged to `main` by PR #3 as
  `5dc99027a460a7f29d9b4021a982af726f2363f2`
- Governing spec: `INVESTMENT_OS_MASTER_SPEC.md`, especially sections 2.2–2.5, 5.1–5.3,
  6.3, 8.2, 14.1, 16.3, 18.1–18.2, and scenario S5
- Related accepted ADRs: ADR-0001, ADR-0003, ADR-0008, ADR-0010, ADR-0011
- Business behavior permitted: immutable Thesis creation/versioning, evidence-backed impact
  evaluation, controlled monitoring/invalidation, time-travel reads, and internal read APIs only

## Objective

Deliver an evidence-first Thesis Engine vertical slice. Each new ThesisVersion is append-only,
has deterministic semantic-diff and provenance, and keeps prior versions readable. New Evidence
may propose an impact only through explicit, deterministic conditions; it must never overwrite a
Thesis or produce a Decision, approval, execution, or live-trading behavior.

## Deliverables

- [x] Strict ThesisVersion internal schema and pure-domain invariants for evidence references,
  version lineage, monitoring, and invalidation conditions
- [x] Thesis create/version/diff service with append-only persistence and optimistic current-pointer
  updates
- [x] Evidence-impact evaluation that creates a new version only for a material, evidence-backed
  change; unchanged Evidence must not create an empty version
- [x] Deterministic monitoring and invalidation condition evaluation, including explicit BROKEN
  escalation candidates without a Decision or order path
- [x] Time-travel Thesis reads and `/api/v1/theses/{instrument_id}` plus version-history read APIs
- [x] Synthetic unit, integration, contract, and S5 regression fixtures
- [x] Data lineage, status, and verification evidence

## Acceptance criteria

1. ThesisVersion records are immutable, content-addressed, unique and monotonically versioned;
   prior versions remain readable after corrections.
2. Every factual pillar, catalyst, risk, or material update retains valid Evidence references;
   missing or unavailable Evidence fails closed.
3. Semantic diff is deterministic for equal inputs and an unchanged impact cannot manufacture a
   new ThesisVersion.
4. An Evidence-backed invalidation creates a new `BROKEN` ThesisVersion and a machine-readable
   forced-review / REDUCE-or-EXIT candidate, without a Decision, approval, execution, or order.
5. Reads at a requested as-of time return only the latest ThesisVersion available then and never
   rewrite historical meaning.
6. The API rejects malformed or instruction-like payloads; fixtures are synthetic only.
7. S5 proves that old versions are unchanged and a historical decision can retain a fixed Thesis
   version reference.

## Verification plan

```text
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest
uv run pytest -m integration --no-cov
uv run python scripts/check_domain_coverage.py
uv run python scripts/export_openapi.py --check
uv run python scripts/export_policy_schema.py --check
docker compose config
uv run pip-audit
uv run pip-licenses --format=markdown --with-urls
uv run python scripts/check_secrets.py
```

## Out of scope

- LLM/Agent runtime, Committee, portfolio/risk/sizing, Decision, approval, execution, scheduler,
  UI, Learning, real provider credentials, and live trading;
- changing investment limits, Risk Veto, approval gates, or any accepted ADR.

## Risks and human decisions

No human investment-governance decision is required for the synthetic, non-decision Thesis slice.
Real retention, provider authorization, and any action after a BROKEN Thesis remain future human
or later-stage concerns.

## Verification evidence

- 2026-09-19: `uv run ruff format --check .` — passed (91 files).
- 2026-09-19: `uv run ruff check .` — passed.
- 2026-09-19: `uv run mypy src` — passed (37 source files).
- 2026-09-19: `uv run pytest` — passed (137 tests; total coverage 93.25%).
- 2026-09-19: `uv run pytest -m integration --no-cov` — passed (19 tests).
- 2026-09-19: `uv run python scripts/check_domain_coverage.py` — passed (99.27% line,
  87.50% branch coverage).
- 2026-09-19: `uv run python scripts/export_openapi.py --check` — passed after regenerating
  `docs/schemas/openapi.json` for the Thesis read endpoints.
- 2026-09-19: `uv run python scripts/export_policy_schema.py --check` — passed.
- 2026-09-19: `docker compose config` — passed.
- 2026-09-19: `uv run pip-audit` — no known vulnerabilities; the local project package is
  skipped because it is not published on PyPI.
- 2026-09-19: `uv run pip-licenses --format=markdown --with-urls` — passed.
- 2026-09-19: `uv run python scripts/check_secrets.py` — passed (114 repository files).
- 2026-09-19: Remote GitHub Actions CI [run 35400610248](https://github.com/zmd6s6/JZ-Investment-OS/actions/runs/35400610248)
  passed on review-head commit `3a0660ea7364dd55a8d87fea6b3405952e9b0444` (quality, Compose
  smoke, and independent Gitleaks jobs).

The stage intentionally contains no Decision, approval, execution, brokerage, or live-trading
path. The S5 regression asserts that a new evidence-backed BROKEN version retains the old version,
emits only a forced-review REDUCE/EXIT candidate, and creates no Decision.
