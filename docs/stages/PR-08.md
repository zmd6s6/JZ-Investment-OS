# PR-08 — Scheduler, Reports & Personal UI

- State: `READY_FOR_REVIEW`
- Depends on: accepted PR-07, merged to `main` by PR #7 as
  `2838301827fd386f38dba5f58ab49a88db26d912`
- Governing spec: `INVESTMENT_OS_MASTER_SPEC.md`, especially sections 3.2–3.3, 14, 16.3–16.4,
  17.1–17.2, 18–19 (S12), 21, and 22
- Related accepted ADRs: ADR-0001, ADR-0003, ADR-0008, ADR-0010, ADR-0011
- Business behavior permitted: synthetic scheduler/replay, report, and personal read-only UI
  workflows, including explicit synthetic U.S., SSE, and SZSE calendar sessions. `auto_trade=false`,
  Risk Veto, approval gates, and the disabled live adapter remain mandatory.

## Objective

Deliver the personal daily operating loop: explicit-market-calendar scheduled work, durable replay
and failure visibility, human-readable reports, and four read-only personal UI views without
creating a live execution path.

## Deliverables

- [x] Daily, weekly, monthly, and quarterly job definitions with explicit market timezone/calendar
- [x] Durable idempotency, advisory locking, as-of replay, bounded recovery, and visible TaskRuns
- [x] Synthetic daily, weekly, and monthly reports that label facts, calculations, judgments,
  assumptions, and human decisions
- [x] React/TypeScript `web/` workspace and the Portfolio, Opportunities, Watchlist, and Decision
  Journal pages
- [x] Prominent simulation, data-as-of, evidence-freshness, Risk Veto, pending-approval, and
  operational-exception indicators
- [x] Normal, failure, and boundary coverage for S12 and each user-visible scheduler/UI safety state

## Acceptance criteria

1. Jobs never use server-local time as business time; each has explicit market-calendar and `as_of`
   semantics, an idempotency key, advisory lock, durable TaskRun, transactional outbox event, and
   replay path.
2. A retry or concurrent invocation of the same logical Daily job has exactly one business effect;
   failures are bounded, sanitized, durable, and visible.
3. Reports distinguish facts, deterministic calculations, Agent judgments, assumptions, and human
   decisions, and never present a recommendation as an executed trade.
4. All four UI pages are usable with synthetic/de-identified data and prominently retain the V1
   simulation/no-auto-trade, freshness, Veto, approval, and exception signals.
5. No scheduler trigger, API/UI action, or report path can bypass Risk Veto, valid human approval,
   immutable history, or disabled live execution.

## Verification plan

```text
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest
uv run pytest -m integration --no-cov
uv run python scripts/check_domain_coverage.py
uv run python scripts/export_openapi.py --check
uv run python scripts/export_policy_schema.py --check
docker compose config --quiet
uv run pip-audit
uv run python scripts/check_secrets.py
```

## Out of scope and human gates

Real brokerage execution, credentials, real Portfolio data, real investment-policy choices, and
Learning proposal activation remain forbidden. A-share synthetic calendar semantics are approved;
a real exchange calendar/data-provider authorization or a change to investment governance still
requires explicit human approval.

## Verification evidence

- 2026-09-20: Review-blocker remediation wired the optional synthetic, explicit-calendar scheduler
  into the worker runtime. A bounded poll processes only already-closed caller-supplied sessions in
  UTC order; it emits durable Daily/Weekly/Monthly/Quarterly TaskRuns, a simulation-only Daily
  report, immutable events, and transactional outbox records. No calendar is inferred or enabled by
  default. `uv run ruff format --check` and `uv run ruff check` over the changed scheduler/worker
  files, `uv run mypy src`, `uv run pytest -m integration --no-cov -q
  tests/integration/test_worker_runtime_scheduler.py tests/integration/test_daily_report_delivery.py
  tests/integration/test_persistence_and_jobs.py` (12 passed), and `uv run pytest --no-cov -q`
  over the scheduler/worker/report unit slice (28 passed) all passed. The runtime test invokes the
  worker cycle twice and proves exactly one Daily report, four successful cadence TaskRuns, four
  outbox records, and no duplicate effect. Corrected the PR-07 merge SHA to
  `2838301827fd386f38dba5f58ab49a88db26d912` in stage/status records and updated the README's
  capability boundaries.
- 2026-09-20: The application image now carries the development-only
  `config/synthetic-runtime-calendar.json` example so a Compose worker can be explicitly started
  with `INVESTMENT_OS_WORKER_SCHEDULE_CALENDAR_PATH=/app/config/synthetic-runtime-calendar.json`;
  the default Compose environment leaves this unset and therefore performs no scheduled business
  work.
- 2026-09-21: Final post-remediation verification passed serially: `uv run pytest -q` (306 tests,
  93.96% total coverage), `uv run pytest -m integration --no-cov -q` (31 integration tests),
  `uv run python scripts/check_domain_coverage.py` (97.96% domain-line and 86.75% domain-branch),
  Ruff format/check, mypy, OpenAPI and Policy Schema drift checks, Compose config, pip-audit, and
  secret scanning. The earlier parallel test invocation was discarded as a database-fixture
  contention artifact; no code failure was observed in the serial run.
- 2026-09-20: Compose runtime smoke passed with `docker compose up --build --detach --wait` and a
  separately invoked worker using
  `INVESTMENT_OS_WORKER_SCHEDULE_CALENDAR_PATH=/app/config/synthetic-runtime-calendar.json`.
  PostgreSQL recorded four successful TaskRuns for the explicit `2026-01-02` session, four
  simulation-only events/outbox records, and one Daily report; the second poll reused the same
  idempotency keys. The disposable local Compose volume was removed after verification.
- 2026-09-20: Full PR-08 verification passed: `uv run ruff format --check .`, `uv run ruff
  check .`, `uv run mypy src`, `uv run pytest -q` (302 tests), `uv run pytest -m integration
  --no-cov -q` (31 integration tests), `uv run python scripts/check_domain_coverage.py` (97.96%
  domain-line and 86.75% domain-branch coverage), `uv run python scripts/export_openapi.py
  --check`, `uv run python scripts/export_policy_schema.py --check`, `docker compose config
  --quiet`, `uv run pip-audit`, and `uv run python scripts/check_secrets.py`. The initially stale
  committed OpenAPI artifact and detect-secrets baseline were regenerated/reviewed as part of this
  verification; the baseline contains only development placeholder findings.
- 2026-09-20: Completed the four-page Chinese personal workspace. Portfolio, Opportunities,
  Watchlist, and Decision Journal now provide an explicit synthetic daily-use workflow instead of
  placeholder text, while retaining report data-as-of, evidence freshness, Risk Veto,
  pending-approval, operational-exception, and no-auto-trade signals on every view. Browser tests
  cover valid, unavailable, malformed, and bounded report states plus navigation safety signals;
  `npm test`, `npm run build`, and `npm audit --json` pass (4 tests, production build, zero
  vulnerabilities). No mutation, approval, scheduler-trigger, or execution UI was introduced.
- 2026-09-20: Remote CI passed for the review-ready branch head
  `f4fbcc4fb24fd6590821301ef6e8f69effa1bb8a`: GitHub Actions CI run `35517315151` completed
  successfully. The branch remains `READY_FOR_REVIEW`; no merge or direct `main` update occurred.

- 2026-09-20: Added the first vertical slice: an explicit `America/New_York` market-session
  contract with no implicit weekday/server-time fallback, UTC scheduled instants, `as_of` validation,
  and stable idempotency keys. The Windows runtime lacked IANA zone data, so the standard `tzdata`
  runtime dependency is now pinned in `uv.lock`; this is required for the explicit-calendar contract
  and does not authorize external market data.
- 2026-09-20: `uv run ruff format --check src/investment_os/application/schedule.py
  tests/unit/test_schedule.py`, `uv run ruff check src/investment_os/application/schedule.py
  tests/unit/test_schedule.py`, `uv run mypy src`, and `uv run pytest --no-cov -q
  tests/unit/test_schedule.py` — PASS (3 tests). Coverage includes a normal half-day session,
  a missing/non-trading date, duplicate calendar data, cross-session `as_of`, and naive timestamp
  rejection.
- 2026-09-20: Added deterministic Daily/Weekly/Monthly/Quarterly eligibility planning. Each
  cadence is emitted only for an explicit known session; weekly/monthly/quarterly work is emitted
  only at that supplied calendar's final session for the respective period. No weekday, holiday, or
  server-clock inference is used.
- 2026-09-20: Added a validated daily/weekly/monthly report contract. Facts require Evidence
  references, deterministic calculations require input hashes, Agent judgments require Opinion
  references, and human decisions require approval/decision references. Rendered reports carry a
  fixed `SIMULATION / NO AUTO TRADE` banner and never turn a recommendation into an execution path.
- 2026-09-20: Added the one-page Daily report structure required by the scheduler contract:
  action-required, continue-holding, watch, new-discoveries, and data/operational-exception
  sections are always present (including explicit empty state) and retain statement provenance.
- 2026-09-20: `uv run ruff format --check src/investment_os/application/reports.py
  tests/unit/test_reports.py`, `uv run ruff check src/investment_os/application/reports.py
  tests/unit/test_reports.py`, `uv run mypy src`, and `uv run pytest --no-cov -q
  tests/unit/test_reports.py` — PASS (9 tests). The Daily report covers normal rendering, explicit
  empty sections, and missing/duplicate-section failure paths.
- 2026-09-20: `uv run ruff format --check src/investment_os/application/reports.py
  tests/unit/test_reports.py`, `uv run ruff check src/investment_os/application/reports.py
  tests/unit/test_reports.py`, `uv run mypy src`, and `uv run pytest --no-cov -q
  tests/unit/test_reports.py` — PASS (7 tests). Normal rendering and provenance labels, missing
  provenance failure paths, and naive/blank boundary inputs are covered.
- 2026-09-20: Bootstrapped the ADR-0001-deferred React/TypeScript `web/` boundary with four
  read-only navigation views. The synthetic preview always exposes `SIMULATION / NO AUTO TRADE`,
  data-as-of, Evidence freshness, Risk Veto, pending-approval, and operational-exception signals.
  It deliberately contains no order or approval mutation controls; API-backed read models remain
  outstanding before the UI deliverable can be marked complete.
- 2026-09-20: `npm run build` from `web/` — PASS (TypeScript compilation and Vite production build).
- 2026-09-20: `npm audit --omit=dev` from `web/` — PASS (0 production vulnerabilities).
- 2026-09-20: Added browser-level regression coverage that switches each personal page and asserts
  the fixed simulation, active-Veto, pending-approval, and operational-exception signals remain
  visible.
- 2026-09-20: `npm test`, `npm run build`, and `npm audit --json` from `web/` — PASS (1 browser
  regression test, production build, and 0 production/development vulnerabilities). The initial
  Vitest 3 transitive advisory was remediated by upgrading to Vitest 5.0.1 before recording this
  evidence.
- 2026-09-20: Connected the UI operational-exception tile to the read-only TaskRun API. It accepts
  only recognized status tokens for presentation and fails closed with an explicit unavailable
  message; it never uses response content to submit actions.
- 2026-09-20: Added a multi-stage API image build that produces the React static bundle and serves
  it at the API origin. The UI remains read-only and obtains TaskRun status from same-origin API
  routes; no CORS exception, credential, or order route was introduced.
- 2026-09-20: During Compose verification, the stale separately tagged migration image could not
  locate the already-applied PR-07 migration. The root cause is fixed by assigning the API, worker,
  and one-shot migration service one shared local image tag, so a rebuilt application image cannot
  run migrations from an older image.
- 2026-09-20: `docker compose config --quiet`, `docker compose build investment-api`, and
  `docker compose run --rm --no-deps investment-migrate alembic heads` — PASS; the image recognizes
  `20260920_0004` as the migration head. `docker compose up --detach --wait investment-api` then
  passed: the same-origin UI root and `/api/v1/task-runs` both returned HTTP 200, and the deployed
  bundle contains the simulation/no-action safety messages. The Docker build context excludes local
  environments, data, UI dependencies, and generated output.
- 2026-09-20: `npm test`, `npm run build`, and `npm audit --json` from `web/` — PASS (2 browser
  tests, production build, and 0 vulnerabilities). The browser tests cover failed and unavailable
  TaskRun states as well as safety signals across all four pages.
- 2026-09-20: Added a scheduler dispatch bridge that sends an explicit calendar-bound `as_of`,
  idempotency key, hash-bound input, and correlation ID to the existing advisory-locked durable
  executor. `dry_run` exposes a non-mutating dispatch plan and never invokes a handler.
- 2026-09-20: Added a bounded, read-only `/api/v1/task-runs` view over durable TaskRuns. It exposes
  sanitized failure metadata and retry attempt counts for the UI without adding any job mutation or
  execution endpoint.
- 2026-09-20: `uv run ruff format --check src/investment_os/infrastructure/scheduler.py
  src/investment_os/api/app.py src/investment_os/api/schemas.py tests/unit/test_task_run_api.py`,
  `uv run ruff check` over the same files, `uv run mypy src`, and `uv run pytest --no-cov -q
  tests/unit/test_task_run_api.py` — PASS (2 tests). The API exposes a sanitized durable failure and
  rejects an out-of-bounds limit before invoking the reader.
- 2026-09-20: `uv run ruff format --check src/investment_os/infrastructure/scheduler.py
  tests/unit/test_scheduler_dispatcher.py`, `uv run ruff check
  src/investment_os/infrastructure/scheduler.py tests/unit/test_scheduler_dispatcher.py`,
  `uv run mypy src`, and `uv run pytest --no-cov -q tests/unit/test_scheduler_dispatcher.py` —
  PASS (3 tests). Dispatch mapping, non-mutating dry-run, and invalid input-hash rejection are
  covered.
- 2026-09-20: Connected the one-page Daily report to the existing durable executor through a
  transactional Event/Outbox handler. The immutable event records its UTC `as_of`, content hash,
  rendered synthetic presentation, and `simulation_only=true`; the paired outbox message contains
  only delivery identifiers and the content hash. The handler adds no execution or approval path.
- 2026-09-20: `uv run ruff format --check src/investment_os/application/reports.py
  src/investment_os/infrastructure/report_delivery.py
  tests/integration/test_daily_report_delivery.py`, `uv run ruff check` over the same files,
  `uv run mypy src`, and `uv run pytest -m integration --no-cov -q
  tests/integration/test_daily_report_delivery.py` — PASS (1 test). The integration test verifies
  exactly one immutable event and one outbox record across a normal Daily job plus an identical
  retry, including the fixed simulation banner and hash-bound delivery payload.
- 2026-09-20: Added the Daily-report schedule binding. It accepts only a `DAILY` explicit
  ScheduledJob whose business `as_of` exactly equals the report's `as_of`, derives the executor
  input hash from immutable rendered content, and carries one correlation ID through delivery.
  A wrong cadence or time drift is rejected before dispatch.
- 2026-09-20: `uv run ruff format --check
  src/investment_os/infrastructure/daily_report_scheduler.py
  tests/unit/test_daily_report_scheduler.py`, `uv run ruff check` over the same files,
  `uv run mypy src`, and `uv run pytest --no-cov -q
  tests/unit/test_daily_report_scheduler.py` — PASS (3 tests). Coverage confirms handler/hash
  binding and both pre-dispatch failure boundaries (wrong cadence and mismatched `as_of`).
- 2026-09-20: Added a bounded, read-only latest-Daily-report API sourced only from immutable
  Daily-report events. It validates the persisted hash, timezone-aware `as_of`, synthetic-only
  marker, and rendered content shape; a missing report returns 404 and a malformed event fails
  closed with a sanitized 503. The endpoint has no scheduler, approval, or execution mutation.
- 2026-09-20: `uv run ruff format --check src/investment_os/infrastructure/report_delivery.py
  src/investment_os/api/app.py src/investment_os/api/schemas.py tests/unit/test_daily_report_api.py`,
  `uv run ruff check` over the same files, `uv run mypy src`, and `uv run pytest --no-cov -q
  tests/unit/test_daily_report_api.py tests/unit/test_task_run_api.py` — PASS (4 tests). Coverage
  includes normal synthetic report presentation plus missing and malformed report failure states.
- 2026-09-20: The personal UI now derives its displayed Daily-report `as_of` from the same-origin,
  read-only latest-report API. Only a valid simulation-marked, parseable timestamp is accepted;
  unavailable or invalid responses retain a prominent stale-data safeguard. The UI still has no
  approval, scheduler, or execution mutation control.
- 2026-09-20: `npm test`, `npm run build`, and `npm audit --json` from `web/` — PASS (3 browser
  tests, production build, 0 vulnerabilities). Browser coverage includes a valid report timestamp,
  malformed-report fail-closed rendering, report/API unavailability, and persistent Risk Veto.
- 2026-09-20: Bounded durable recovery to three attempts per idempotency key. After the final
  handler failure, a further invocation does not run the handler, retains `FAILED`, and records the
  sanitized `JOB_RETRY_LIMIT_EXHAUSTED` condition in the durable TaskRun for operational visibility.
- 2026-09-20: `uv run ruff format --check src/investment_os/application/errors.py
  src/investment_os/infrastructure/persistence/jobs.py tests/integration/test_persistence_and_jobs.py`,
  `uv run ruff check` over the same files, `uv run mypy src`, and `uv run pytest -m integration
  --no-cov -q tests/integration/test_persistence_and_jobs.py` — PASS (8 tests). Integration
  coverage confirms normal idempotency, rollback, lock contention, input drift, and bounded retry
  exhaustion without a fourth business-handler invocation.
- 2026-09-20: The owner approved A-share inclusion for synthetic research/simulation scope. ADR-0012
  adds `SSE` and `SZSE` venue semantics using `Asia/Shanghai`, alongside the existing U.S. venue.
  Calendar session dates and close times remain explicit inputs; no weekday inference, market-data
  provider, real portfolio, policy parameter, credential, or live-trading capability was added.
- 2026-09-20: `uv run ruff format --check src/investment_os/application/schedule.py
  tests/unit/test_schedule.py`, `uv run ruff check` over the same files, `uv run mypy src`, and
  `uv run pytest --no-cov -q tests/unit/test_schedule.py` — PASS (6 tests). Coverage includes an
  SSE 15:00 `Asia/Shanghai` close mapped to its correct UTC instant, with `as_of` and idempotency
  remaining tied to the Shanghai market date.
- 2026-09-20: Strengthened Daily-report replay: the latest-report reader now recomputes the SHA-256
  content address of the stored snapshot and requires the fixed simulation banner before exposing
  it. A later malformed/tampered event does not silently fall back to stale content; the API remains
  fail-closed through its existing sanitized unavailable response.
- 2026-09-20: `uv run ruff format --check src/investment_os/infrastructure/report_delivery.py
  tests/integration/test_daily_report_delivery.py`, `uv run ruff check` over the same files,
  `uv run mypy src`, and `uv run pytest -m integration --no-cov -q
  tests/integration/test_daily_report_delivery.py` — PASS (2 tests). The integration slice verifies
  a valid immutable snapshot replay and rejection of a later hash-mismatched snapshot.
- 2026-09-20: The four read-only personal views now include a Daily-report snapshot preview rather
  than only its `as_of`. Both the persistence reader and UI enforce a 12,000-character bound, a
  non-empty simulation banner, a valid timestamp, and `simulation_only=true`; invalid or unavailable
  content is presented as an explicit no-action operational exception.
- 2026-09-20: `uv run ruff format --check src/investment_os/infrastructure/report_delivery.py`,
  `uv run ruff check src/investment_os/infrastructure/report_delivery.py`, `uv run mypy src`, and
  `uv run pytest -m integration --no-cov -q tests/integration/test_daily_report_delivery.py` — PASS
  (2 tests). `npm test`, `npm run build`, and `npm audit --json` from `web/` — PASS (4 browser
  tests, production build, 0 vulnerabilities), including valid bounded snapshot preview and invalid
  snapshot safety rendering.
- 2026-09-20: Added as-of Daily-report replay. The immutable-event reader selects only the newest
  verified report whose business timestamp is no later than the requested timezone-aware `as_of`;
  no matching snapshot is a visible 404 rather than a fallback to future information.
- 2026-09-20: `uv run ruff format --check src/investment_os/infrastructure/report_delivery.py
  src/investment_os/api/app.py tests/unit/test_daily_report_api.py
  tests/integration/test_daily_report_delivery.py`, `uv run ruff check` over the same files,
  `uv run mypy src`, `uv run pytest --no-cov -q tests/unit/test_daily_report_api.py`, and
  `uv run pytest -m integration --no-cov -q tests/integration/test_daily_report_delivery.py` — PASS
  (3 unit and 3 integration tests). Coverage verifies API handoff, a valid as-of snapshot, and the
  boundary before the first report without reading a future report.
- 2026-09-20: Daily-report section ordering is now a validated canonical contract: action required,
  continue holding, watch, new discoveries, then data/operational exceptions. This makes otherwise
  equivalent reports render and hash deterministically instead of depending on caller iteration
  order.
- 2026-09-20: `uv run ruff format --check src/investment_os/application/reports.py
  tests/unit/test_reports.py`, `uv run ruff check` over the same files, `uv run mypy src`, and
  `uv run pytest --no-cov -q tests/unit/test_reports.py` — PASS (10 tests), including the
  noncanonical-section-order boundary.
- 2026-09-20: Localized the personal UI and synthetic report presentation to Chinese: navigation,
  page content, status tiles, operational exceptions, and report section/provenance labels are now
  Chinese. The fixed English `SIMULATION / NO AUTO TRADE` phrase remains alongside Chinese safety
  text to preserve existing snapshot integrity validation; immutable historical snapshots are not
  rewritten.
- 2026-09-20: `uv run ruff format --check src/investment_os/application/reports.py
  tests/unit/test_reports.py`, `uv run ruff check` over the same files, `uv run mypy src`,
  `uv run pytest --no-cov -q tests/unit/test_reports.py`, and `uv run pytest -m integration
  --no-cov -q tests/integration/test_daily_report_delivery.py` — PASS (10 unit and 3 integration
  tests). `npm test`, `npm run build`, and `npm audit --json` from `web/` — PASS (4 browser tests,
  production build, 0 vulnerabilities).
