# PR-00 — Repository Constitution & Bootstrap

- State: `READY_FOR_REVIEW`
- Depends on: none
- Governing spec: `INVESTMENT_OS_MASTER_SPEC.md`, especially sections 3, 17, 18, 20, 21, 22, 23, and 24
- Business behavior permitted: health/smoke paths only

## Objective

Create a reproducible, secure repository foundation on which PR-01–PR-09 can be implemented. This stage establishes engineering capability; it does not implement investment decision logic.

## Required deliverables

- [x] Master Spec at repository root
- [x] Root `AGENTS.md`
- [x] Working mode and current-status files
- [x] Repository README with exact local setup and verification commands
- [x] Python 3.12+ project metadata and locked dependencies
- [x] Node/React metadata deferred to PR-08 by ADR-0001
- [x] Required source/test/docs directory skeleton
- [x] API and worker health/smoke entry points with no fake business behavior
- [x] PostgreSQL/API/worker Compose configuration and health checks
- [x] `.env.example` with placeholders only
- [x] `.gitignore`, formatting, lint, typing, test, and coverage configuration
- [x] CI for Python checks, tests, Compose validation, secret scanning, and dependency/license checks
- [x] ADR-0001 through ADR-0010 initial decisions
- [x] One reproducible smoke test
- [x] PR-00 delivery evidence and rollback instructions

## Acceptance criteria

1. A clean machine can follow README instructions to install dependencies, start the required services, and run the smoke test.
2. Canonical format, lint, type, unit/smoke, and Compose validation commands pass.
3. CI runs the same canonical checks and does not require production secrets.
4. No route, service, test, or mock claims that investment behavior exists.
5. Domain code is either absent or limited to empty package boundaries; PR-01 behavior is not pulled forward.
6. Secret scanning finds no committed credential; `.env` is ignored.
7. Each ADR has the required structure and a real decision, not only a title.
8. `docs/PROJECT_STATUS.md` truthfully reflects verification and remaining work.

## Required verification evidence

Record exact command, date, result, and relevant output summary:

| Check | Command | Result | Evidence |
|---|---|---|---|
| Formatting | `uv run ruff format --check .` | PASS | 43 files unchanged, 2026-09-17 |
| Lint | `uv run ruff check .` | PASS | All checks passed, 2026-09-17 |
| Types | `uv run mypy src` | PASS | 14 source files, 2026-09-17 |
| Tests | `uv run pytest` | PASS | 12 passed; 87.12% total coverage, 2026-09-17 |
| OpenAPI | `uv run python scripts/export_openapi.py --check` | PASS | Committed contract current, 2026-09-17 |
| Compose syntax | `docker compose config --quiet` | PASS | Configuration valid, 2026-09-17 |
| Dependency audit | `uv run pip-audit` | PASS | No known dependency vulnerabilities; local project not on PyPI, 2026-09-17 |
| License inventory | `uv run pip-licenses --format=markdown --with-urls` | PASS | Inventory generated; no unreviewed runtime copyleft license found, 2026-09-17 |
| Secret scan | `uv run python scripts/check_secrets.py` | PASS | 58 repository files; only baselined local placeholder credential, 2026-09-17 |
| Compose smoke | `docker compose up --build --detach --wait` then `uv run python scripts/smoke.py` | PASS | PostgreSQL, API, and worker healthy; database probe and `live_trading=false` verified on port 8100; logs clean; volume retained, 2026-09-17 |
| Remote CI | `.github/workflows/ci.yml` | NOT VERIFIED | Remote exists, but no push or remote PR was requested |

## Out of scope

- Investment Policy behavior and state machines;
- database domain tables and migrations beyond infrastructure readiness;
- DSA integration;
- runtime investment Agents;
- Risk, sizing, decisions, approvals, reports, UI, scheduler, and Learning Engine.

## Human gates

No investment-policy decision is expected. Any proposal to enable live execution, choose real portfolio limits, or weaken Master Spec constraints must be rejected as out of scope.

## Active limitations

1. **Remote CI evidence** — `origin` is configured, but this work has not been pushed. Run the
   workflow when the user explicitly requests publication; do not claim remote CI success before then.

## Completion record

Fill when moving to `READY_FOR_REVIEW`:

- Scope completed: all PR-00 repository artifacts, local checks, image build, Compose startup, service health, end-to-end smoke, clean-log inspection, and controlled shutdown.
- Spec clauses implemented: Master Spec sections 3, 17, 18, 20, 21 PR-00, 22, 23, and 24 within bootstrap scope.
- Verification summary: local quality, test, OpenAPI, Compose syntax/runtime, dependency, license, and secret checks pass; remote CI remains unverified under the blocker above.
- Known limitations: no investment behavior exists; CI has not executed remotely. The API host port defaults to 8100 so it can coexist with the existing DSA web service on port 8000.
- Rollback/recovery: follow `README.md`; no domain migration or real portfolio data exists.
- Human decisions required: none. An explicit user instruction is required before connecting or pushing a remote.
- Recommended next stage: the user explicitly reprioritized to PR-01 on 2026-09-17. PR-01 proceeded
  with local/container PR-00 evidence green and remote CI transparently left `NOT VERIFIED`.
