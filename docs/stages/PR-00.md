# PR-00 — 仓库宪法与引导

- 状态：`READY_FOR_REVIEW`
- 依赖：无
- 治理规范：`INVESTMENT_OS_MASTER_SPEC.md`，重点为第 3、17、18、20、21、22、23、24 节
- 允许的业务行为：仅健康/smoke 路径

## 目标

建立可复现、安全的仓库基础，以便实现 PR-01–PR-09。本阶段建立工程能力；不实现投资决策逻辑。

## 必需交付物

- [x] 仓库根目录的主规范
- [x] 根 `AGENTS.md`
- [x] 工作模式和当前状态文件
- [x] 含准确本地设置和验证命令的仓库 README
- [x] Python 3.12+ 项目元数据和锁定依赖
- [x] 根据 ADR-0001 将 Node/React 元数据延后至 PR-08
- [x] 必需的源码/测试/文档目录骨架
- [x] 没有伪造业务行为的 API 和 Worker 健康/smoke 入口
- [x] PostgreSQL/API/Worker Compose 配置和健康检查
- [x] 只含占位符的 `.env.example`
- [x] `.gitignore`、格式化、lint、类型、测试和覆盖率配置
- [x] Python 检查、测试、Compose 校验、密钥扫描及依赖/许可证检查的 CI
- [x] ADR-0001 至 ADR-0010 的初始决策
- [x] 一项可复现的 smoke 测试
- [x] PR-00 交付证据和回滚说明

## 验收标准

1. 全新机器可按 README 安装依赖、启动所需服务并运行 smoke 测试。
2. 规范格式、lint、类型、单元/smoke 和 Compose 校验命令通过。
3. CI 运行同一规范检查且不需要生产密钥。
4. 没有路由、服务、测试或 mock 声称已具备投资行为。
5. 领域代码不存在，或仅限空包边界；不得提前引入 PR-01 行为。
6. 密钥扫描找不到已提交凭据；`.env` 被忽略。
7. 每个 ADR 都有必需结构和真实决策，而非仅有标题。
8. `docs/PROJECT_STATUS.md` 如实反映验证和剩余工作。

## 必需验证证据

记录准确命令、日期、结果和相关输出摘要：

| 检查 | 命令 | 结果 | 证据 |
|---|---|---|---|
| 格式化 | `uv run ruff format --check .` | PASS | 43 个文件未变，2026-09-17 |
| Lint | `uv run ruff check .` | PASS | 全部检查通过，2026-09-17 |
| 类型 | `uv run mypy src` | PASS | 14 个源文件，2026-09-17 |
| 测试 | `uv run pytest` | PASS | 12 项通过；总覆盖率 87.12%，2026-09-17 |
| OpenAPI | `uv run python scripts/export_openapi.py --check` | PASS | 已提交契约为最新，2026-09-17 |
| Compose 语法 | `docker compose config --quiet` | PASS | 配置有效，2026-09-17 |
| 依赖审计 | `uv run pip-audit` | PASS | 无已知依赖漏洞；本地项目未发布到 PyPI，2026-09-17 |
| 许可证清单 | `uv run pip-licenses --format=markdown --with-urls` | PASS | 已生成清单；未发现未审查的运行时 copyleft 许可证，2026-09-17 |
| 密钥扫描 | `uv run python scripts/check_secrets.py` | PASS | 58 个仓库文件；仅有已基线化的本地占位凭据，2026-09-17 |
| Compose smoke | `docker compose up --build --detach --wait` 后执行 `uv run python scripts/smoke.py` | PASS | PostgreSQL、API、Worker 健康；已在 8100 端口验证数据库探针和 `live_trading=false`；日志干净，卷保留，2026-09-17 |
| 远程 CI | `.github/workflows/ci.yml` | PASS | 累积分支运行 `35206014268` 全部作业通过，2026-09-17 |

## 范围外

- Investment Policy 行为和状态机；
- 除基础设施就绪性外的数据库领域表及迁移；
- DSA 集成；
- 运行时投资 Agent；
- Risk、仓位规模计算、决策、批准、报告、UI、调度器和 Learning Engine。

## 人类闸门

预期不需要投资政策决定。任何启用实盘执行、选择真实组合限额或削弱主规范约束的提案都必须作为范围外拒绝。

## 当前限制

不存在 PR-00 工程 blocker。等待人类阶段验收。

## 完成记录

移至 `READY_FOR_REVIEW` 时填写：

- 已完成范围：所有 PR-00 仓库工件、本地检查、镜像构建、Compose 启动、服务健康、端到端 smoke、干净日志检查和受控关闭。
- 已实现规范条款：引导范围内的主规范第 3、17、18、20、21（PR-00）、22、23、24 节。
- 验证摘要：本地质量、测试、OpenAPI、Compose 语法/运行时、依赖、许可证和密钥检查均通过；累计远程 CI 运行 `35206014268` 通过。
- 已知限制：PR-00 本身不含投资行为。API 主机端口默认 8100，以便与 8000 端口上的现有 DSA Web 服务共存。
- 回滚/恢复：遵循 `README.md`；不存在领域迁移或真实组合数据。
- 需要人类决定：无。所有者随后授权阶段分支推送；直接推送或合并到 `main` 仍未授权。
- 建议下一阶段：用户于 2026-09-17 明确重新排序至 PR-01。PR-01 在本地/容器验证后推进，并随后通过累计远程 CI。
