# ADR-0014：LLM 成本与 Token 预算门禁

- 状态：已接受
- 日期：2026-09-22

## 决策

所有真实模型请求必须在应用层先取得不可超卖的预算预留。预算策略、模型定价和账本任一不可用时，网关失败关闭，绝不发送网络请求。预算同时覆盖单任务累计 Token/成本与 UTC 日累计 Token/成本；同一 `request_id` 的修复尝试属于同一任务累计。

定价由所有者通过模型档案配置，包含币种、输入/输出 Token 单价、版本和生效时间；实现不得硬编码任何提供方价格。预留与日/任务账本在同一数据库事务、行锁下更新。响应中可验证的实际用量会结算为追加式 `llm_usage` 记录，并把预留转换为已消费；调用失败或用量不可验证时保留预留，采取保守计费。

## 替代方案

- 只使用每次请求的 `max_tokens`：不能约束修复尝试、日累计或成本，拒绝。
- 在响应后才统计用量：无法阻止超限请求，拒绝。
- 在代码中维护供应商价格表：会产生过期和治理不透明风险，拒绝。

## 安全与运维影响

预算服务不读取或记录凭据，且不能绕过模型的 HTTPS、结构化输出或 Evidence 边界。日级事务咨询锁牺牲少量并发度以确保硬上限不被并发预留超卖；失败时保留预留，因此可能保守地减少可用额度，但不会低估风险。

## 迁移与回滚

迁移 `20260922_0007_llm_budget_governance` 新增档案定价字段和预算表，不修改既有投资记录。生产问题采用前向修复；未部署环境可以按 Alembic 顺序降级。迁移不包含凭据、真实价格或个人数据。

## 引用

- `INVESTMENT_OS_MASTER_SPEC.md` §6.5、§17.5
- `docs/stages/PRODUCT-03.md`
- ADR-0004、ADR-0010、ADR-0013

## 后果

模型连接测试也是模型网络请求，因此同样受预算和定价门禁约束。默认 UI 值 `TEST_DEFAULT` 仅供测试配置，未保存不代表有有效预算。真实网络验证仍须由所有者用合成 Evidence 和其自行录入的凭据执行。
